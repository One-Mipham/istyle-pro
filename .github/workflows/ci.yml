name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  # ── API + Shared: typecheck & tests ──
  api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - run: pnpm install --filter @istyle/shared --filter @istyle/api
      - run: pnpm --filter @istyle/shared typecheck
      - run: pnpm --filter @istyle/shared test
      - run: pnpm --filter @istyle/api typecheck
      - run: pnpm --filter @istyle/api test

  # ── Mobile: typecheck & tests ──
  mobile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - run: pnpm install --filter @istyle/shared --filter @istyle/mobile
      - run: pnpm --filter @istyle/shared build
      - run: pnpm --filter @istyle/mobile typecheck
      - run: pnpm --filter @istyle/mobile test
