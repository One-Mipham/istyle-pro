// Image caching and local storage utilities for iStyle Pro

export async function cacheImage(uri: string, key: string): Promise<string> {
  // Placeholder for image caching — Phase 1 uses direct URIs
  return uri;
}

export async function getCachedImage(key: string): Promise<string | null> {
  return null;
}

export function clearImageCache(): void {
  // Placeholder
}
